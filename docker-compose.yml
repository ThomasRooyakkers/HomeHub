version: "3.9"
services:
  invoice-tracker:
    build: .
    container_name: invoice-tracker
    restart: unless-stopped
    ports:
      - "3000:80"
    volumes:
      - invoice_data:/data

volumes:
  invoice_data:
